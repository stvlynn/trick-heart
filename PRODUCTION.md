# Lynn 版《トリックハート》制作记录

交付文件：`output/Trick-Heart-Lynn-PV.mp4`。

以 `trick-heart-original.mp4` 为底片，按照 `oc-lynn.jpeg` 的人物特征将原片两个人类角色替换成 Lynn。完整保留 157 秒、24 fps 的剪辑时间线；原 AAC 音轨直接复制。最终规格为 1920×1080，3768 帧。

## 固定设定

共同特征：银白齐下巴短发、青蓝内层发梢、蓝眼睛、细圆框眼镜、两枚侧发夹、蓝色缎带。

- A 魔术师：黑色菱形纹高帽、青蓝帽带、深海军蓝礼服外套、青蓝袖口与领结、白衬衫与手套、黑短裤、不对称黑白长袜、深蓝短靴。
- B 日常：黑贝雷帽、白色长袖水手领衬衫、青蓝蝴蝶结、海军蓝百褶裙、双银色轨道扣、白短袜、黑乐福鞋。

固定视觉参考：`assets/lynn/duo-010.png`。角色替换清单：`work/timeline.json`，包含 60 个合成层与 55 份角色素材。孤立手部、衣袖与片尾衣服局部另行统一配色。

## 制作方式和画面限制

使用内置 image_gen 制作各镜头的 Lynn 素材，并在原视频上做跟踪、裁切、遮挡和颜色合成。原片的主要背景、字幕、道具、动物、剪辑及音轨继续沿用。人物使用逐镜头关键姿势与仿射运动合成，细微表情和肢体动作有简化，部分与人物相交的道具、背景边缘会有重绘差异。

### 抛球段（1:43–1:52.6）

原片这一段是逐帧重绘的有限动画：整段 230 帧由 31 张不同的原画循环构成，头、肩、领结每张都略有差异。早期版本把 Lynn 头像连同部分肩膀整块贴到仍在运动的原片身体上，再用特征点估算缩放，导致肩线频繁错位。现在改为：

- 只替换帽子、头发、脸和颈部（`scripts/juggling.py`），肩膀、手臂、手套、球和衬衫领子全部保留原片动作，原片红色礼服和黑领结按镜头整帧重着色为深蓝与青蓝。
- `scripts/track_juggling.py` 先把帧归并成 31 张原画，在每张原画上定位黑色领结顶端作为锚点，把头像的颈部对齐到该锚点；近景镜头的缩放比（1.59）由参考原画的脸部多尺度匹配得出。结果写入 `work/shoulder-fix/head-tracks.json`，`work/shoulder-fix/anchor-qc.jpg` 是逐原画的锚点核对图。
- 原角色的头发、皮肤通过颜色连通域动态擦除，避免重绘漂移时露出红发；只有位于脸部凸包之外的大块白色/橙色区域才被当作手套、袖口还原到前景，原角色的眼白不会残留。

### 横条边框段（0:29.3–0:32.8）

这两个镜头的画面是一条手绘横条，其上下墨线每帧都在抖动。早期版本整块贴入素材，素材自带的静止边框在贴片边界（x=1410 与 x=770）处与原片抖动的边框错开一截。现在时间线中这两层标记 `follow_band`（`scripts/composite.py` 的 `band_edges` / `Plate.follow_band`）：逐列从原片检测横条的上下墨线（原角色头发盖住墨线的列由相邻列插值），素材只填充墨线以内，墨线本身与横条外的红底、飘落花瓣全部沿用原片；原角色溢出横条的头发清为红底并补回墨线。检查图：`work/band-fix/after.jpg`。

### 纸杯传声段（0:58.4–1:08.0）

四个镜头（`s042`–`s045`）的纸杯连线横跨整个画面，并且每帧都在抖动；素材里自带的一段静止连线在贴片边界（x=1070、450、1490）处与原片连线错开约 10 px，素材还把 58.5 s 那一帧的闪光记号和一半歌词字「失」一起画了进去。这四层现在标记 `restore_overlays` 与 `static`（`scripts/composite.py` 的 `strip_plate_overlays` / `restore_overlays`）：

- 素材里紧邻原片连线的细长横向结构（即素材自己画的连线）和远离人物的小块孤立图形（闪光、半个字）先擦成红底。
- 原片中的连线（细白线加墨边，按粗细和长度筛选，排除手套、帽饰等粗白色块）与歌词文字（桃色细笔画，排除同色的毛衣），连同它们的运动模糊边缘，在 Lynn 轮廓之外的所有位置整体还原；连线因此在贴片边界处与原片完全一致，并且每帧跟随原片抖动。
- 这几个镜头机位固定，改为 `static`，去掉了交替帧上 SIFT 估计带来的几像素抖动。

检查图：`work/string-fix/after.jpg`。

`output/` 中带 `preview`、`review`、`render`、`patched`、`polish`、`shoulder`、`band`、`string` 的文件为中间检查文件；交付文件名为 `Trick-Heart-Lynn-PV.mp4`。

### 袖口与短促表情段（0:16.375–0:18.5、0:22.25–0:22.75）

- 新绘 `assets/lynn/s008-016.500.png`：上方为深蓝礼服袖、青蓝翻折袖口和白手套，下方为白色水手服袖及双线袖口。画面两侧歌词取自原片，保留出现时间。
- 原先 `s015-022.300.png` 覆盖了整段 12 帧，遗漏后续闭眼握拳的姿势变化。现在按原片零起算帧号分为 534–536、537–539、540–545 三组，新增 `frame-537.png` 和 `frame-540.png` 两张原画；两侧歌词继续使用原片。
- 增量合成清单：`work/cuff-pose-fix/patch.json`。以已修复肩颈、横条边框和纸杯连线的完整成片为基础更新。

### 背影摆动段（0:32.792–0:35.375）

原先整段复用 `s021-033.200.png`，遗漏原片循环中的其他背影姿势。逐帧对照后，将 787–848 共 62 帧按四组原画的实际出现顺序划分成 11 段，补绘 `frame-787.png`（向左大幅摆动）、`frame-790.png`（向右回摆）、`frame-802.png`（向左过渡），中间姿势继续使用原背影素材。机位固定，使用静态放置避免特征点跟踪引入额外缩放；人物区域外的歌词与背景保留原片。逐帧映射见 `work/pose33-fix/mapping.json`，合成清单见 `work/pose33-fix/patch.json`，检查图见 `work/pose33-fix/after.jpg`。

## 验证

- 完整解码 3768 帧，1920×1080、24 fps、视频 157 秒。
- AAC 音轨与原片直接复制后的 ADTS 字节 SHA-256 相同。
- 检查全部镜头锚点、切换前后帧、镜头中点、未覆盖区间以及跟踪低置信度位置。
- 最终逐秒画面接触表：`work/delivery-qc/page-*.jpg`。
- 机器验证报告：`work/delivery-qc/verification.json`。

合成程序：`scripts/composite.py`（抛球段头像放置见 `scripts/juggling.py` 与 `scripts/track_juggling.py`）。验证程序：`scripts/verify_delivery.py`。保留所有素材和时间线，可继续细化。

### 动作、切镜与前景复核（本轮多点反馈）

新增 30 张 Lynn 原画（四张 `frame-864/870/876/882.png` 与 26 张 `repair-*.png`），将 25 个旧时间线层重排为 97 个修复片段。原始时间线快照为 `work/motion-audit/timeline-before.json`，构建脚本为 `scripts/build_motion_repairs.py`，增量合成清单为 `work/motion-audit/patch.json`。

覆盖 0:36、0:39–0:41、0:45–0:54、1:00 起纸杯原地变化、1:30 入退场、1:40 花朵反应、1:55 抛球、1:56 风线、2:05 袖口、2:17 底图及切镜、2:20 沙发姿势。兔子出帽原片帧 849–861 保留，人物特写从 862 开始。大幅姿势用独立原画，小幅同姿势变化用局部稠密光流逐帧映射；光流仅变形素材，不替代对语义姿势的补绘。抛球复现保留原片身体、手及球，另行跟踪 Lynn 头像。

前景处理增加原片画框、文字粒子、白闪与风线的恢复；袖口使用两套服装的补绘。原片音轨直接复制。CV 审计及人工复核范围、局限、额外候选见 `work/motion-audit/REPORT.md`，机器报告位于同目录的 `before/`、`after/`。额外候选仍需细化，不能将检测候选数量当作已修复问题数量。

### 抛球整个人物重绘及明确转头（第二轮）

1:43–1:56.042 的旧方案采用静态头像、原片身体与调色手臂，造成头身动作不协调；且替换层在第 2769 帧合帘开始时结束，原角色重新露出。现用 13 张完整人物原画 `assets/lynn/juggle-v2-*.png`，同时替换头、颈、肩、衣服、手及球的关键姿势。依据原片领口位置对齐整个原画并按完整身体姿势匹配，覆盖远景、左侧近景、右侧近景；不再将同一闭眼头像附到原片身体上。原片字幕和边框保留。全身合成持续至第 2784 帧，幕布以原片前景遮挡，背景取色排除深红幕布。

2:19.833–2:21.542 新绘正面、向画面右侧、向画面左侧三张头部方向明确的沙发姿势，小鸟也按方向转动。按头部区域与原片匹配，时间段为 3356–3362 正面，3363–3369 右，3370–3376 左，3377–3384 右，3385–3396 左。避免用背景粒子决定人物姿势。

新增 16 张原画。构建脚本 `scripts/build_juggle_v2.py`，全身合成 `scripts/juggle_body.py`，时间线及逐帧映射 `work/juggle-v2/patch.json`、`body-tracks.json`。旧版成片保留为 `output/Trick-Heart-Lynn-PV-before-juggle-v2.mp4`。

### 0:19 独立近景补绘

18.708333–19.666667 秒（帧 449–471）改用四张独立素材：扶帽面部、背影、牵手及躯干、双人全景，分别为 `assets/lynn/pose19-450/456/460/466.png`。按原片切点保持 5、5、5、8 帧，不再由全景放大裁切复用近景，也不加入跟踪变形。构建脚本 `scripts/build_pose19_fix.py`，清单与成片检查图位于 `work/pose19-fix/`。

额外检查的 18.5–18.708333 秒腿部近景因图像生成额度耗尽，未取得新素材，本轮保持旧版；不计入四张补绘。成片完整解码 3768 帧，原始 AAC 字节校验一致。替换前备份为 `output/Trick-Heart-Lynn-PV-before-pose19.mp4`。
